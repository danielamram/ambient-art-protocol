# @ambient/web

Phase 5: a Vite + React app that renders the full-screen WebGL2 canvas from `@ambient/engine`,
mounts sources from `@ambient/sources`, and shows a floating overlay to tweak signals by hand and
switch shader themes.

Not scaffolded yet, on purpose: keeping Vite out of the install until the engine exists keeps
Phase 1 lean.
